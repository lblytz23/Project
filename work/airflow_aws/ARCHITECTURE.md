# 系统架构说明

## 🏗️ 架构概览

```
┌─────────────────────────────────────────────────────────────────┐
│                        AWS Cloud                                 │
│                                                                   │
│  ┌───────────────────────────────────────────────────────┐      │
│  │  MWAA (Managed Workflows for Apache Airflow)          │      │
│  │                                                         │      │
│  │  ┌──────────────────────────────────────────────┐     │      │
│  │  │  DAG: gpu_server_cicd_workflow               │     │      │
│  │  │                                               │     │      │
│  │  │  1. validate_inputs                          │     │      │
│  │  │  2. retrieve_ssh_key ──────┐                │     │      │
│  │  │  3. test_ssh_connection     │                │     │      │
│  │  │  4. clone_and_checkout_repo │                │     │      │
│  │  │  5. read_version_yaml       │                │     │      │
│  │  │  6. update_airflow_variables│                │     │      │
│  │  │  7. cleanup_temp_files      │                │     │      │
│  │  └─────────────────────────────┼────────────────┘     │      │
│  │                                 │                       │      │
│  └─────────────────────────────────┼───────────────────────┘      │
│                                    │                               │
│  ┌─────────────────────────────────▼──────────────────────┐      │
│  │  AWS Secrets Manager                                   │      │
│  │  ┌──────────────────────────────────────────┐          │      │
│  │  │  Secret: gpu-server-ssh-key              │          │      │
│  │  │  - SSH Private Key                       │          │      │
│  │  │  - ARN: arn:aws:secretsmanager:...       │          │      │
│  │  └──────────────────────────────────────────┘          │      │
│  └─────────────────────────────────────────────────────────┘      │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐      │
│  │  VPC                                                     │      │
│  │  ┌───────────────┐  ┌──────────────────────────┐       │      │
│  │  │ Private       │  │ VPN Gateway / Peering    │       │      │
│  │  │ Subnets       │──│ Connection               │───────┼──┐   │
│  │  │               │  │                          │       │  │   │
│  │  │ - MWAA        │  └──────────────────────────┘       │  │   │
│  │  │ - Airflow     │                                     │  │   │
│  │  └───────────────┘                                     │  │   │
│  └─────────────────────────────────────────────────────────┘  │   │
│                                                                │   │
│  ┌─────────────────────────────────────────────────────────┐  │   │
│  │  S3 Bucket                                              │  │   │
│  │  ┌────────────────────────────────────────┐             │  │   │
│  │  │  dags/                                 │             │  │   │
│  │  │    └─ gpu_server_cicd_dag.py           │             │  │   │
│  │  │  plugins/                              │             │  │   │
│  │  │    └─ plugins.zip                      │             │  │   │
│  │  │  requirements.txt                      │             │  │   │
│  │  │  config/                               │             │  │   │
│  │  │    └─ config.yaml                      │             │  │   │
│  │  └────────────────────────────────────────┘             │  │   │
│  └─────────────────────────────────────────────────────────┘  │   │
│                                                                │   │
└────────────────────────────────────────────────────────────────┼───┘
                                                                 │
                    Private Link / VPN                           │
                            │                                    │
                            ▼                                    │
┌────────────────────────────────────────────────────────────────▼───┐
│                    本地网络 / GPU服务器                             │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  GPU Server (10.x.x.x)                                   │     │
│  │                                                           │     │
│  │  ┌───────────────────────────────────────────┐           │     │
│  │  │  /home/ubuntu/workspace/                  │           │     │
│  │  │    └─ cicd-test/   (Git Repository)       │           │     │
│  │  │         ├─ www/                           │           │     │
│  │  │         │   └─ version.yaml               │           │     │
│  │  │         └─ ... (other files)              │           │     │
│  │  └───────────────────────────────────────────┘           │     │
│  │                                                           │     │
│  │  ┌───────────────────────────────────────────┐           │     │
│  │  │  SSH Service (Port 22)                    │           │     │
│  │  │  - Authentication: SSH Key                │           │     │
│  │  │  - User: ubuntu                           │           │     │
│  │  └───────────────────────────────────────────┘           │     │
│  │                                                           │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

## 🔄 数据流

### 1. DAG触发阶段
```
用户触发DAG
    ↓
输入参数 (gpu_server_ip, workspace_path, secrets_arn)
    ↓
验证参数 (validate_inputs)
```

### 2. 密钥获取阶段
```
retrieve_ssh_key
    ↓
访问 AWS Secrets Manager
    ↓
获取SSH私钥
    ↓
保存到临时文件 (/tmp/airflow_ssh_*/ssh_private_key.pem)
    ↓
设置文件权限 (chmod 600)
```

### 3. SSH连接阶段
```
test_ssh_connection
    ↓
建立SSH连接到GPU服务器
    ↓
执行测试命令 (whoami)
    ↓
验证连接成功
```

### 4. Git操作阶段
```
clone_and_checkout_repo
    ↓
SSH连接到GPU服务器
    ↓
创建工作目录 (mkdir -p workspace)
    ↓
检查仓库是否存在
    ↓
克隆Git仓库 (git clone)
    ↓
切换分支 (git checkout cicd_01_etl)
    ↓
验证当前分支
```

### 5. 版本读取阶段
```
read_version_yaml
    ↓
SSH连接到GPU服务器
    ↓
读取文件 (cat workspace/cicd-test/www/version.yaml)
    ↓
解析YAML内容
    ↓
返回版本数据字典
```

### 6. 变量更新阶段
```
update_airflow_variables
    ↓
遍历版本数据
    ↓
更新Airflow Variables (a, b, c, d)
    ↓
记录更新结果
```

### 7. 清理阶段
```
cleanup_temp_files
    ↓
删除临时SSH密钥文件
    ↓
删除临时目录
```

## 🔐 安全架构

### 网络安全层
```
Internet
    ↓
AWS VPN Gateway / VPC Peering
    ↓
VPC Security Group (Egress: Allow SSH to GPU IP)
    ↓
Private Link / VPN Tunnel
    ↓
GPU Server Security Group (Ingress: Allow SSH from MWAA VPC)
    ↓
GPU Server
```

### 认证授权层
```
IAM User/Role
    ↓
MWAA Execution Role
    ├─ S3 Access (Read DAGs/Plugins)
    ├─ Secrets Manager Access (GetSecretValue)
    ├─ CloudWatch Logs (Write logs)
    └─ Airflow API (Execute DAGs)
    ↓
SSH Key Authentication
    ├─ Stored in Secrets Manager
    ├─ Encrypted at rest
    └─ Retrieved at runtime
    ↓
GPU Server Access
```

## 📦 组件说明

### 核心组件

#### 1. DAG文件 (`gpu_server_cicd_dag.py`)
- **作用**: 定义工作流程和任务
- **位置**: S3 bucket/dags/
- **刷新**: MWAA自动检测更新（3-5分钟）

#### 2. 插件模块

**gpu_ssh_utils.py**
- SSH连接管理
- 命令执行
- 文件上传/下载
- 连接池管理

**aws_secrets_utils.py**
- Secrets Manager交互
- 密钥获取
- 密钥创建/更新
- 错误处理

#### 3. 配置文件

**config.yaml**
- 默认配置
- 环境特定配置
- SSH/Git/AWS参数

**requirements.txt**
- Python依赖
- 版本锁定

### 支持组件

#### S3 Bucket
- 存储DAG和插件
- 版本控制启用
- 生命周期策略

#### Secrets Manager
- SSH密钥存储
- 自动轮换支持
- 访问审计

#### CloudWatch
- DAG执行日志
- 任务日志
- 系统指标
- 告警配置

#### IAM
- 执行角色
- 权限策略
- 信任关系

## 🎯 任务依赖图

```
validate_inputs
      ↓
retrieve_ssh_key
      ↓
test_ssh_connection
      ↓
clone_and_checkout_repo
      ↓
read_version_yaml
      ↓
update_airflow_variables
      ↓
cleanup_temp_files
```

所有任务串行执行，确保每一步完成后再执行下一步。

## 🔄 扩展架构

### 支持多GPU服务器

```python
# 使用TaskGroup和动态任务映射
from airflow.utils.task_group import TaskGroup

gpu_servers = [
    {"ip": "10.0.1.100", "workspace": "/home/ubuntu/ws1"},
    {"ip": "10.0.1.101", "workspace": "/home/ubuntu/ws2"},
]

for server in gpu_servers:
    with TaskGroup(f"process_{server['ip']}"):
        # 为每个服务器创建任务组
        ...
```

### 并行Git仓库

```python
# 克隆多个仓库
repos = ["cicd-test", "model-repo", "data-repo"]

clone_tasks = [
    clone_repo.override(task_id=f"clone_{repo}")(repo)
    for repo in repos
]
```

### 集成其他AWS服务

```
MWAA
  ├─ S3 (数据存储)
  ├─ SageMaker (模型训练)
  ├─ Lambda (轻量级任务)
  ├─ SNS (通知)
  ├─ Step Functions (复杂工作流)
  └─ EventBridge (事件驱动)
```

## 📊 监控架构

### 日志层次

```
Application Logs
  ├─ DAG Processing Logs
  ├─ Scheduler Logs
  ├─ Task Logs
  └─ Worker Logs
      ↓
CloudWatch Logs
      ↓
Metrics & Alarms
      ↓
SNS Notifications
```

### 指标收集

```
MWAA Metrics
  ├─ DAG Success Rate
  ├─ Task Duration
  ├─ Queue Length
  └─ Worker CPU/Memory
      ↓
Custom Metrics
  ├─ SSH Connection Time
  ├─ Git Clone Duration
  └─ Variable Update Count
```

## 🚀 性能优化

### 连接池
- 重用SSH连接
- 减少连接开销

### 并行任务
- 多GPU服务器并行处理
- 独立任务并行执行

### 缓存
- Git仓库增量更新
- 密钥缓存（短期）

### 资源调优
- Worker数量配置
- 任务并发度
- 超时时间设置

## 📚 相关文档

- [README.md](README.md) - 项目概述
- [QUICKSTART.md](QUICKSTART.md) - 快速开始
- [MWAA_SETUP.md](MWAA_SETUP.md) - 环境配置
- [examples/](examples/) - 使用示例

